/** @type {import('next').NextConfig} */
const nextConfig = {
  // Aqui ficam as configurações do seu site
  // Se você tiver alguma configuração de imagens ou redirects, coloque aqui
  reactStrictMode: true,
};

export default nextConfig;
